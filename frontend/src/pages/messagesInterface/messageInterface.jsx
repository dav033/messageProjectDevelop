import "./messageInterface.scss";
import { Link } from "react-router-dom";
import useAuth from "../../auth/useAuth";
export default function MessageInterface() {
  const { logout } = useAuth();
  return (
    <div className="messageInterface">
      
     
    </div>
  );
}
